#1.5
# Пункт a.
a = matrix(c(4.3, 3.1, 8.2, 8.2, 3.2, 0.9, 1.6, 6.5), 
           nrow=4, ncol=2, byrow=TRUE)
a

# Пункт b.
dim(a[-1,])

# Пункт c.
a[,2] = sort(a[,2])
a

# Пункт d.
matrix(a[-4,-1])

# Пункт e.
e <- a[3:4,]
e

# Пункт f.
a[c(4,1), c(2,1)] = 1/2 * diag(e)
a
